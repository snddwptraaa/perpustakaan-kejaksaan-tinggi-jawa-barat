<div class="space-y-6">
    <div><p class="text-xs font-bold uppercase tracking-[0.2em] text-kejati-gold-dark">Keamanan</p><h1 class="mt-2 text-3xl font-semibold tracking-tight">Audit log</h1><p class="mt-2 text-sm text-slate-500">Jejak perubahan penting yang dilakukan petugas.</p></div>
    <div class="rounded-2xl border border-stone-200 bg-white shadow-sm"><div class="border-b border-stone-100 p-4"><label for="audit-search" class="sr-only">Cari audit log</label><input id="audit-search" wire:model.live.debounce.300ms="search" placeholder="Cari aksi atau entitas…" class="w-full max-w-md rounded-xl border-stone-200 bg-stone-50 text-sm"></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><caption class="sr-only">Jejak audit perubahan sistem</caption><thead class="bg-stone-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Waktu</th><th class="px-6 py-4">Petugas</th><th class="px-6 py-4">Aksi</th><th class="px-6 py-4">Entitas</th><th class="px-6 py-4">IP</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse ($logs as $log)<tr><td class="whitespace-nowrap px-6 py-4">{{ $log->created_at->format('d M Y H:i') }}</td><td class="px-6 py-4">{{ $log->user?->name ?? 'Sistem' }}</td><td class="px-6 py-4 font-semibold text-kejati">{{ ucfirst($log->aksi) }}</td><td class="px-6 py-4 font-mono text-xs">{{ $log->entitas }}{{ $log->entitas_id ? '#'.$log->entitas_id : '' }}</td><td class="px-6 py-4 font-mono text-xs text-slate-500">{{ $log->ip_address ?: '—' }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-slate-500">Belum ada audit log.</td></tr>@endforelse</tbody></table></div><div class="border-t border-stone-100 px-6 py-4">{{ $logs->links() }}</div></div>
</div>
